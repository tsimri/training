<?php
    echo "<h1>Cwiczenia z GITa</h1>";
?>
<div>
    <ol>
        <li>Zmodyfikować plik <i>index.php</i> dodając nagłówek <code>h3</code>
            z bieżącą datą (sprawdzić jak pobrac bieżącą datę w PHP)</li>
        <li>Wykonać komendę* <code>git status</code></li>
        <li>Wykonać komendę* <code>git add --all</code></li>
        <li>WYkonać komendę* <code>git commit -am "Twój komentarz"</code></li>
        <li>Wykonać komendę* <code>git branch</code> </li>
        <li>WYkonać komendę* <code>git checkout -b poboczna_galaz</code></li>
        <li>Wykonać komendę <code>git status</code></li>
        <li>Zmodyfikować plik <i>index.php</i> zmienić color tła na <i>#ccc</i></li>
        <li>Wykonać komendy <code>git add --all</code> i <code>git commit -am</code></li>
        <li>Wykonać komendę <code>git checkout master</code> co się stało? ;)</li>
        <li>Wykonać komendę <code>git log</code></li>
        <li>Poczytać o komendzie <code>git pull</code> i <code>git push</code></li>
        <li>Poczytać o komendzie <code>git revert</code></li>
    </ol>
</div>

<h2 style="color: red">
    * - Najpierw poczytaj o tej komendzie
</h2>
